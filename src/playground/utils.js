console.log('utils.js is runnign');

export const add = (a,b) => a+b; // export in line export na frente

export const square = (x) => x*x; // export

// const subtract = (a,b) => a-b;

export default (a,b) => a-b;

// export {square, add, subtract as default };

// export { square,add }; // not object

// exports - default exports  - named exports 
